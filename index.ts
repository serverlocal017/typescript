function echo(title: string): string {
    return title;
}

function echoYear(year: number): number {
  return year;
}

function echoArg(arg: any): any {
  return arg;
}

echo('Subscribe');
echoYear(20);

echoArg('Hello');
echoArg(2);
